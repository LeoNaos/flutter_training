import 'package:flutter/material.dart';

class ProfileDetailTabbar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Profile Detail'),
      ),
      body: const Center(
        child: Text('Profile Detail Screen'),
      ),
    );
  }
}
